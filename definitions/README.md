This sub-project defines the boundaries of the logical application (the interactor) and its components.

Only interfaces are allowed here, and the implementations are found [here](url "../components").